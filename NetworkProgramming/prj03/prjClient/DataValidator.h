#ifndef __DATAVALIDATOR__
#define __DATAVALIDATOR__


bool is_valid_ip(char *ipaddr);

bool is_valid_port(char *c_port, unsigned short *port);

bool is_valid_nickname(char *nickname);

#endif // !__DATAVALIDATOR__

